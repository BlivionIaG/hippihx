#include "hippihx/tile_stub.hpp"

// Contract: attention/dsa_nope — DSA MLA-NoPE class (not V4 compressed Lightning).
// LDS / __launch_bounds__ locked in README.md when the real tile lands.
__global__ void hippihx_attention_dsa_nope_stub() {}
